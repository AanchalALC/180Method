/* ============================================================================
   BLOG CONFIG — Sanity project coordinates, committed (no secrets: the
   dataset is public and we only ever read published documents).

   projectId is a PLACEHOLDER until the Sanity project exists. Replace it
   here AND in 180method-studio/sanity.config.js + sanity.cli.js — all three
   must match the real project ID from manage.sanity.io.
   ========================================================================== */
export const projectId = 'ruxd82na'
export const dataset = 'production'
export const apiVersion = '2026-08-19'
export const POSTS_PER_PAGE = 9
