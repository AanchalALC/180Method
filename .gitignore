node_modules
dist
dist-ssr
.vite
*.local
.DS_Store
.env
.env.*
!.env.example
npm-debug.log*

# Build artefact — regenerated by `npm run content` / `npm run build` from
# Sanity. Never hand-edit; never commit.
src/content/posts.json
